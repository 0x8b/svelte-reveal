import RevealNotes from 'reveal.js/plugin/notes/notes.js'

export default {
  app: {
    name: 'Reveal.js with Svelte and Tailwind'
  },
  reveal: {
    disableLayout: true,
    plugins: [RevealNotes]
  }
}
